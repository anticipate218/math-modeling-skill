# 贡献指南

欢迎通过 Issue 或 Pull Request 改进本技能。本指南说明具体的贡献原则与流程。

---

## 一、内容原则（最重要）

本技能是**规则追踪项目**，其价值在于"准确记录官方规则"而非"给建议"。贡献必须遵循：

### 1.1 每条规定必须标注来源可信度

- **`[官方]`**：来自竞赛组织方文件或官网原话，需附链接与原文引用页码/条款号。
- **`[半官方]`**：来自国家级媒体报道、历年赛后讲评视频、官方 FAQ，需注明出处。
- **`[社区]`**：来自获奖论文语料统计、公开经验贴，需说明样本量与局限性。

未标注来源的规定不会被合并。

### 1.2 "官方未公开"的项目必须明确说明

例如：
- ✅ 正确："官方从未公布评分权重表，唯一公开标准是《章程》第二条的四维定性描述。"
- ❌ 错误："评分标准是：假设 30%、建模 40%、求解 20%、文字 10%。"（编造）

如果只能找到社区流传的说法（如"美赛评分表"），必须加 `[社区]` 并说明"非官方"。

### 1.3 规则变更的处理方式

竞赛规则每年都会变（尤其是 AI 相关条款）。报告规则变更时请：
1. **提供新旧文件的对照**：附新版链接 + 具体变更条款号与原文对比。
2. **说明生效时间**：如"2026 年试行"/"2025 年起"等。
3. **更新受影响的文件**：如 AI 规定变更，需同步改 `contests.md`、`checklists.md`、`check_paper.py`。

用 Issue 模板"规则更新"提交，或直接发 PR（附上述信息）。

---

## 二、技术贡献

### 2.1 改进 `check_paper.py`

- 新增检查项须有**明确依据**（官方规定 or 评委公开表述）。
- 每个 FAIL 级检查必须附 `hint`（说明依据与修正方向）。
- 添加固件到 `SELF_TEST_CASES`，确保 `--self-test` 通过。
- WARN 级检查可基于获奖论文语料统计（需注明样本量）。

### 2.2 改进 `validate_skill.py` 或其它脚本

- 保持纯标准库（不添加外部依赖）。
- 退出码语义明确：0=通过，1=错误，2=使用错误。
- 非交互式（CI 友好）。

### 2.3 添加 `references/` 文档或 `assets/`

- 新文档须在 `SKILL.md` 的"参考文件索引"表中加一行。
- 外部链接必须是稳定的（官网 > GitHub releases > 网盘链接）。
- 示例代码/模板须可直接运行（或注明依赖）。

---

## 三、流程

### 3.1 报告问题

用 Issue 模板"规则更新"或"Bug 报告"提交；描述问题时请附：
- **具体文件与行号**（如 `contests.md:42`）
- **官方原文截图或链接**（规则类问题必须）
- **期望行为 vs 实际行为**（Bug 类问题必须）

### 3.2 提交 Pull Request

1. Fork 本仓库，在你的分支改动。
2. **运行本地检查**（必须全部通过才能提 PR）：
   ```bash
   python scripts/validate_skill.py .
   python scripts/check_paper.py --self-test
   ```
3. 提交信息格式：`feat/fix/docs: 简短描述（≤50 字符）`
4. PR 描述须说明：改了什么、为什么改、依据是什么（附链接）。

### 3.3 代码风格

- Python 遵循 PEP 8（允许 88 字符行宽）。
- Markdown 用中文标点（，。；：）、英文数字两侧空格。
- 文件末尾保留一个空行。

---

## 四、不接受的贡献（明确拒绝，避免浪费你的时间）

- ❌ **编造的"评分标准"**：如果官方未公布，就诚实地说"未公布"。
- ❌ **"我觉得应该……"的建议**：除非有官方依据或获奖论文统计支持。
- ❌ **添加外部依赖**：`check_paper.py` 与 `validate_skill.py` 必须保持纯标准库。
- ❌ **自动生成完整论文**：本技能的定位是"规则助手 + 自检工具"，不是"代写论文"。
- ❌ **闭源/付费内容的整合**：必须是可公开验证的资料。

---

## 五、维护者承诺

- **24 小时内响应**有效的规则变更 Issue（含官方链接）。
- **7 天内合并**符合上述原则的 PR（或给出具体修改建议）。
- **每年 9 月前更新**当年最新规则（国赛/研赛通常在 8 月公布次年规则，美赛在 11 月）。

---

感谢你帮助这个技能保持准确与时效！
